var peso = 39;
var fa = `Voce é um Frango!`;
var fo = `é forte pra caralhooo!`;
var peso =80

if(peso >= 40){
    console.log(`Voce foi aprovado, pois ${fo}`);
}
else{
    console.log(`Voce foi reprovado, pois ${fa}`);
}